/**
 * Позже подключим Google Sheets / Apps Script endpoint.
 * Сейчас — чтобы структура была готова.
 */

export async function submitGuestForm(payload) {
    // TODO: заменить на реальный endpoint
    console.log("submitGuestForm payload:", payload);
  
    // имитация ответа
    return { ok: true };
  }
  